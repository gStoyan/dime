[project]
name = "dime"
version = "0.1.0"
requires-python = ">=3.13"

[project.scripts]
dime = "dime_cli.main:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"